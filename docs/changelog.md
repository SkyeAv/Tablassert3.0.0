# Changelog

The canonical release history lives in the repository root at [`CHANGELOG.md`](https://github.com/SkyeAv/Tablassert/blob/main/CHANGELOG.md).

## Current Release Notes

### 7.3.3 - 2026-04-08

- Datassert shard usage is documented as the current 10-shard layout used by `tablassert`.
- Provenance examples now match the runtime behavior: `repo` provides the namespace prefix and `publication` provides the repository-local identifier.
- API and runtime docs now reflect the current `resolve_many()` return shape and entity-resolution metadata.

For older releases and the full project history, open the root `CHANGELOG.md` in the repository.
